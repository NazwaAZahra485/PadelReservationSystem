Petunjuk singkat

1. cd server && npm install
2. cp .env.example .env (opsional)
3. npm run dev (server di :4000)
4. cd client && npm install
5. npm run dev (client di :5173)

Jika backend belum jalan, frontend tetap menampilkan data dummy untuk preview UI dan chart.
