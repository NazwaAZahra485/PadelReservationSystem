import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' 
import './styles.css' // Pastikan ini mengarah ke file CSS Biru-Kuning yang kita buat

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);